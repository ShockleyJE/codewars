// Attempt 1: Using array.map

let double = (x) => x * 2;

function maps(x){
  let dbldarray = x.map(double);
  return dbldarray
}

// OR, one-liner

function maps(x){
    return dbldarray = x.map((x) => x * 2);
}
